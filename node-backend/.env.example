# Local Node backend settings (used when running node-backend directly)
PORT=3000
GO_BACKEND_URL=http://localhost:8080
NODE_ENV=development
GO_REQUEST_TIMEOUT_MS=5000
GO_MAX_RESPONSE_BYTES=1048576
SHUTDOWN_TIMEOUT_MS=10000
AUTH_ENABLED=true
AUTH_USERNAME=admin
AUTH_PASSWORD=ChangeMe123@
AUTH_API_KEY=dev-local-api-key
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=120
AUTH_RATE_LIMIT_MAX_REQUESTS=10

# Note:
# Docker Compose uses root .env / .env.example instead of this file.
